namespace WPF.Models;

public enum RoomType
{
    Single,
    Double,
    Suite
}

public enum RoomStatus
{
    Available,
    Occupied,
    NeedsCleaning
}

public enum ReservationStatus
{
    Confirmed,
    Cancelled,
    Completed
}
